[project]
name = "parsec-python"
version = "0.1.0"
description = "A Monadic Parser Combinator for Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
lint = [
    "ruff",
    "pyright>=1.1.400",
]
test = []
